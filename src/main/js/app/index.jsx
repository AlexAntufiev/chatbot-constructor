import React from 'react';

const {document} = window;

function render() {
    return (
        <span>todo: index.jsx is not work</span>
    );
}

document.addEventListener('DOMContentLoaded', render);