spring:
  application:
    name: chatbot-constructor
    version: 0.1.1
  devtools:
    add-properties: false
  cloud:
    consul:
      enabled: false
      config:
        watch:
          enabled: false
        format: yaml