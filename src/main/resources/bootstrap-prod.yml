spring:
  cloud:
    consul:
      enabled: true
      host: ec2-18-218-83-47.us-east-2.compute.amazonaws.com
      port: 8500
      discovery:
        healthCheckInterval: 300s
        preferIpAddress: true
      config:
        watch:
          enabled: true
          delay: 1000 #ms
    inetutils:
      ignored-interfaces:
        - docker0