spring:
  cloud:
    consul:
      enabled: true
      discovery:
        preferIpAddress: true
      config:
        watch:
          enabled: true
    inetutils:
      ignored-interfaces:
        - docker0